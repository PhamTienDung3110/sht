/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['hsavnu.edu.vn', 'hsa-education.sgp1.digitaloceanspaces.com'], // Thêm hostname cần thiết vào đây
  },
}

module.exports = nextConfig
